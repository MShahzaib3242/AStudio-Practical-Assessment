import { Link } from "react-router-dom";
import {
  FileIcon,
  FolderIcon,
  GraphIcon,
  HuskyLogoIcon,
  LayoutIcon,
  PieIcon,
} from "../assets";
import Button from "./UI/Button";

const Hero = () => {
  return <section className="py-6 bg-white">Home page</section>;
};

export default Hero;
